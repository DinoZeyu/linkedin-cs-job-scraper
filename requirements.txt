# Core data/NLP
pandas>=2.2
spacy>=3.7
numpy>=1.26

# Parallel + progress
tqdm>=4.66
joblib>=1.3

# HF + finetuning stack
huggingface_hub>=0.24
transformers>=4.43
accelerate>=0.33
peft>=0.11
safetensors>=0.4
sentencepiece>=0.1.99

# GPU helpers (optional but common for LLMs)
bitsandbytes>=0.43 ; platform_system == "Linux"

# Unsloth
unsloth>=2024.6

# Jupyter (optional, but useful for notebooks)
jupyterlab>=4.0
ipykernel>=6.28